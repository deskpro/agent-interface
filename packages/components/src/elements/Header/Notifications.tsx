import * as React from "react";
import Icon from "../Icon/Icon";

const Notifications: React.FC = () => (
  <div className="dp-Notifications">
    <Icon name="notifications" size={21} />
  </div>
);

export default Notifications;
